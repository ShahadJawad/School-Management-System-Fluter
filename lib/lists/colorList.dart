import 'package:flutter/material.dart';
import 'package:grad_projct/constant.dart';

class colors {
  colors({required this.myColors});
  Color myColors;
}

List<colors> colorList =[
 colors(myColors: KColor),
  colors(myColors: Colors.pinkAccent),
  colors(myColors: Colors.teal),
  colors(myColors: Colors.deepPurple),
  colors(myColors: KColor),
  colors(myColors: Colors.pinkAccent),
  colors(myColors: Colors.teal),
  colors(myColors: Colors.deepPurple)
];