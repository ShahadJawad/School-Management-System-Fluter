import 'package:flutter/material.dart';

class classDetails_page extends StatelessWidget {
  const classDetails_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
